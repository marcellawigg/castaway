<% if @homes_show %>
<body>
  <nav class="navbar-transparent navbar-fixed-top navbar-static-top col-sm-12 col-md-12 col-lg-12" >
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "CAST-AWAY", root_path, class: "brand-name-home" %> </a>    </div>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
        <li><%= link_to "Cart (#{@cart.contents.count})", cart_path %></li>
          <li><%= link_to "Logout", logout_path, method: :delete %></li>
          <li><%= link_to(cl_image_tag(current_user.avatar, size: "20x20", crop: :fill), orders_path) %> </li>
        <% else %>
          <li><%= link_to "Login", login_path %></li>
          <li><%= link_to "Cart (#{@cart.contents.count})", cart_path %></li>
          <% end %>
        </ul>
      </div>
    </nav>
    <% else %>
    <div class="container-fluid">
    <nav class="nav navbar-custom col-sm-12 col-md-12 col-lg-12" >
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="brand brand-name-general navbar-left" href="/">CAST-AWAY</a>    </div>
        <ul class="nav navbar-nav navbar-right">
          <% if current_user %>
            <li><%= link_to "Cart (#{@cart.contents.count})", cart_path, class: "nav navbar-custom navbar-right" %></li>
            <li><%= link_to "Logout", logout_path, method: :delete, class: "nav navbar-custom navbar-right" %></li>
            <li><%= link_to(cl_image_tag(current_user.avatar, size: "20x20", crop: :fill), orders_path, class: "nav navbar-custom navbar-right") %> </li></ul>
          <% else %>
          <li class="navbar-custom"><%= link_to "Cart (#{@cart.contents.count})", cart_path %></li>
            <li class="navbar-custom"><%= link_to "Login", login_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </nav>
<% end %>
